apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  labels:
    kubevirt.io/vm: alpine-datavolume
  name: alpine-datavolume
spec:
  running: false
  template:
    metadata:
      labels:
        kubevirt.io/vm: alpine-datavolume
    spec:
      domain:
        devices:
          disks:
          - disk:
              bus: virtio
            name: datavolumedisk1
        resources:
          requests:
            memory: 1Gi
      volumes:
      - dataVolume:
          name: alpine-datavolume-dv
        name: datavolumedisk1
  dataVolumeTemplates:
  - metadata:
      name: alpine-datavolume-dv
    spec:
      pvc:
        storageClassName: nfs
        volumeName: alpine-datavolume-pv
        accessModes:
        - ReadWriteOnce
        resources:
          requests:
            storage: 2Gi
      source:
        http:
          url: https://dl-cdn.alpinelinux.org/alpine/v3.19/releases/x86_64/alpine-standard-3.19.0-x86_64.iso