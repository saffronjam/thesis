Initial:

Everything was smooth sailing (except when I didn't read the instruction properly and missed the provided password). This process for managing VMs is much more direct, transparent and reproducible than using a UI-based tool. For developers with previous kubernetes experience it looks to be a very intuitive way to handle virtual machines. In an environment where kubernetes clusters are already part of the workflow, it is obvious how aspects such as automation and access control can be transferred to apply also to the VM infrastructure.
 
Since I was just trying it out, I haven't had any real issues that I need to debug yet.